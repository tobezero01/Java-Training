spring:
  application:
    name: checkout-service
  config:
    # ← Chỉ rõ URL Config Server trong mạng compose
    import: "optional:configserver:http://config-server:8888"
  cloud:
    config:
      fail-fast: true
      retry:
        initial-interval: 1000
        max-attempts: 20
  datasource:
    url: jdbc:mysql://host.docker.internal:3306/checkout_eshopdb?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: ducnhu1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: checkout-service

  security:
    jwt:
      issuer: eshop-auth
      secret: ${JWT_SECRET:a2VlcC10aGlzLWtleS1wcml2YXRlLWFuZC1hbmQtb25seS1kZXZlbG9wZXItaGF2ZS1wZXJtaXNzaW9uLXRvLWNoYW5nZS1pdC1ub3RpZnktYnktRHVjbmh1YWQ=}
      access-ttl-seconds: 36000

#  mail:
#    host: ${MAIL_HOST:smtp.gmail.com}
#    username: ${MAIL_USER:ducnhuadclone1@gmail.com}
#    password: ${MAIL_PASS:gywe lppj dqpu rrzb}
#    properties.mail.smtp.auth: true
#    properties.mail.smtp.starttls.enable: true
#
#mail:
#  from: ${MAIL_FROM:ducnhuadclone1@gmail.com}
#  fromName: ${MAIL_FROM_NAME:EShop}
outbox:
  publish-interval-ms: 500   # mỗi 500ms scan outbox 1 lần
  batch-size: 50

eureka:
  client:
    service-url:
      defaultZone: http://eureka:8761/eureka
  instance:
    prefer-ip-address: false
app:
  security:
    enabled: true